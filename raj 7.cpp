#include <stdio.h>

int main()
{
    int num;
    
    printf("Enter an integer: ");
    scanf("%d", &num);
    
    printf("The number in hexadecimal format: %X\n", num);
    
    return 0;
}

